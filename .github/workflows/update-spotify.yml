name: Update Spotify SVG

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Generate Spotify SVG
        env:
          SPOTIPY_CLIENT_ID: ${{ secrets.fb783c391d274280b3fb1f811f688a89 }}
          SPOTIPY_CLIENT_SECRET: ${{ secrets.479181a6d7fe47e7a2841c5823b4f63a }}
          SPOTIPY_REDIRECT_URI: http://127.0.0.1:80/callback
        run: |
          python scripts/generate_spotify_svg.py

      - name: Commit & push if changed
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add assets/spotify.svg
          git diff --cached --quiet || git commit -m "🎵 update spotify svg"
          git push
